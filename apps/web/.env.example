# Target used only by the Vite development proxy.
VITE_API_PROXY_TARGET=http://localhost:3000

# Optional API origin for deployed environments. Leave empty to use same-origin /api.
VITE_API_BASE_URL=
